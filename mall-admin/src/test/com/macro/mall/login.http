### 后台用户登录
POST http://localhost:8080/demo/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}

> {%
    // 将返回的 token 自动保存到全局变量，方便后续接口使用
    client.global.set("auth_token", response.body.data.tokenHead + ' ' + response.body.data.token);
%}